stages:
  - build
  - test
  - sonar
  - deploy

build:
  stage: build
  script:
    - cd Software/Backend
    - gradle build -x test

test:
  stage: test
  script:
    - cd Software/Backend
    - gradle test

sonar:
  stage: sonar
  script:
    - cd Software/Backend
    - gradle sonarqube -D "sonar.projectKey=Individual-Track-Spring" -D "sonar.host.url=http://localhost:9000" -D "sonar.login=sqp_40a4ccfdca49a02beafb24b4f64eb00876743cd8"

deploy:
  stage: deploy
  script:
    - cd Software
    - docker-compose up -d
    - docker ps

after_script:
  - echo "End CI"
  - exit
